{
  "installCommand": "npm install",
  "builds": [
    {
      "src": "admin/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    },
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/content/(.*)", "dest": "/content/$1" },
    { "src": "/assets/(.*)", "dest": "/admin/dist/assets/$1" },
    { "src": "/admin$", "dest": "/admin/dist/index.html" },
    { "src": "/admin/(.*)", "dest": "/admin/dist/$1" },
    { "src": "/", "dest": "/admin/dist/index.html" },
    { "src": "/(.*)", "dest": "/admin/dist/$1" }
  ]
}
