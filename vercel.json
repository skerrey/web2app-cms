{
  "buildCommand": "mkdir -p public && cd admin && npm install && npm run build && cp -r dist/* ../public/",
  "outputDirectory": "public",
  "functions": {
    "api/*.js": {
      "runtime": "@vercel/node@3"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
